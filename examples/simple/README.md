# Simple ALB Example

This example demonstrates how to deploy an internal Application Load Balancer (ALB) using the `terraform-aws-alb` module.

## Usage

1. Initialize the directory:
   ```bash
   terraform init
   ```

2. Provision the resources:
   ```bash
   terraform apply
   ```
